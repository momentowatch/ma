# Public production URL, including the base path, with a trailing slash.
# Consumed by index.html via Vite's %VITE_SITE_URL% HTML constant replacement
# and by src/i18n/config.ts. Must match Settings -> Pages exactly.
VITE_SITE_URL=https://momentowatch.github.io/ma/
