# النشر (Deployment)

**الرابط الإنتاجي:** https://noureddinelmobaraki-web.github.io/monento/

## اسم المستودع مقابل العلامة التجارية
اسم المستودع على GitHub هو `monento` (بحرف n)، بينما اسم العلامة التجارية هو `MOMENTO` (بحرف m).
المسار الأساسي (`base`) في `vite.config.ts` يجب أن يطابق اسم المستودع حرفياً `/monento/` لكي تعمل روابط الأصول (assets) بشكل صحيح على GitHub Pages.

## الأسرار والمتغيرات
لا يتطلب التطبيق أي أسرار أو متغيرات بيئة (Environment Variables) للعمل، ولا يحتوي على اتصالات قواعد بيانات أو مفاتيح API خاصة.
جميع استدعاءات البيانات والصور تتم بشكل ثابت من مصادر عامة.

## خطوات البناء المحلي

```bash
# تثبيت الاعتماديات
npm install

# فحص الأكواد واختبارها
npm run lint
npm test

# البناء
npm run build

# معاينة البناء
npm run preview
```

## جدول الأعطال

| العطل | السبب المحتمل | الحل |
|---|---|---|
| صفحة بيضاء | مسار `base` غير صحيح | تأكد أن `base` هو `/monento/` في `vite.config.ts` |
| صور مفقودة | خطأ في روابط الـ CDN | تحقق من المستودع `nl-audio-cdn` وتأكد من وجود الصور وتوفر jsDelivr |
| 404 عند تحديث الصفحة | غياب توجيه SPA | تأكد من استخدام التنقل الداخلي (Router) دون تغيير الرابط الفعلي (مثل الوضع الحالي) |
